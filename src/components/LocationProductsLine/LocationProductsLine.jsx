import locationNext from '../../images/icons/locationNext.svg';

const LocationProductsLine = ({ locationProductsLine }) => {
  return (
    <img src={locationNext} alt="locationNext" onClick={locationProductsLine} />
  );
};

export default LocationProductsLine;
